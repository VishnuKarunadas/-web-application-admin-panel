<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #f8f9fa; /* Light background */
      }
      .container {
        max-width: 600px; /* Center the content */
        margin-top: 50px; /* Space from the top */
        padding: 20px;
        background-color: white; /* White background for content */
        border-radius: 8px; /* Rounded corners */
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Subtle shadow */
      }
      h1 {
        color: #343a40; /* Dark text for the heading */
      }
      p {
        color: #495057; /* Medium dark text for the paragraph */
      }
      .btn-signout {
        background-color: #dc3545; /* Bootstrap danger color */
        color: white; /* White text for the button */
        border: none; /* Remove border */
      }
      .btn-signout:hover {
        background-color: #dc3545; /* Same color on hover */
        color: white; /* Ensure text color remains white */
        text-decoration: none; /* Remove underline */
      }
    </style>
  </head>
  <body>
    <div class="container text-center">
      <h1>Welcome to the Home Page</h1>
      <p>You are logged in, <strong><%= user.name %></strong>!</p>

      <a href="/logout" class="btn btn-signout" style="color: white; text-decoration: none; display: inline-block; padding: 10px 20px; background-color: #dc3545; border-radius: 5px; text-align: center;">
        Sign Out
      </a>
      
      
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
